#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <stdio.h>
#include <stdlib.h>

const char*my_strrchr(const char *string, int symbol);
char*my_strdup(char *str);
int my_strlen(char *str);

#endif // FUNCTIONS_H
